# 🧠 BrainStorm — Мультиплеерная викторина с GigaChat

Интерактивная викторина в реальном времени. Хост задаёт тему — GigaChat генерирует вопросы.
Играйте с телефона, компьютера, планшета — всё что нужно это браузер и одна Wi-Fi сеть.

---

## Быстрый старт

### 1. Скачать / распаковать проект

Убедись что структура папок такая:
```
BrainStorm/
├── app/
│   ├── __init__.py
│   ├── ai_client.py
│   ├── game_logic.py
│   ├── routes.py
│   └── socket_events.py
├── static/
│   ├── css/style.css
│   └── js/game.js
├── templates/
│   └── index.html
├── .env
├── requirements.txt
├── run.py
└── start.bat
```

### 2. Настроить .env

Открой файл `.env` и вставь свой ключ GigaChat:

```env
GIGACHAT_CREDENTIALS=вставь_сюда_свой_ключ
GIGACHAT_SCOPE=GIGACHAT_API_PERS
GIGACHAT_MODEL=GigaChat
SECRET_KEY=любая-случайная-строка
HOST=0.0.0.0
PORT=5000
LOG_LEVEL=INFO
```

> Получить ключ: [developers.sber.ru](https://developers.sber.ru/) → войти через Сбер ID → создать приложение → скопировать Authorization Key

### 3. Запустить

**Windows** — двойной клик на `start.bat`

**Или через терминал:**
```bash
python run.py
```

После запуска в терминале появится:
```
+--------------------------------------------------+
|   BrainStorm  —  сервер запущен!                 |
+--------------------------------------------------+
|   Локально:  http://localhost:5000               |
|   По сети:   http://192.168.x.x:5000             |
+--------------------------------------------------+
```

Открой адрес в браузере — и можно играть.

---

## Как играть

### Хост (создатель комнаты)
1. Нажми **«Создать лобби»**, введи имя
2. Настрой игру: тема, количество вопросов, сложность, режим
3. Нажми **«Применить настройки»** — гости увидят изменения сразу
4. Дождись игроков → нажми **«Начать игру»**

### Гость
1. Нажми **«Присоединиться»**, введи имя и код комнаты от хоста
2. Видишь настройки игры — они обновляются в реальном времени
3. Жди старта от хоста

### Игровой процесс
- На каждый вопрос **30 секунд**
- Чем быстрее отвечаешь — тем больше очков
- Серия правильных ответов подряд даёт **стрик-бонус** (+10, +20...)
- Каждые 5 вопросов — промежуточные результаты

---

## Режимы игры

| Режим | Описание |
|-------|----------|
| 🏆 Классика | Все отвечают одновременно, очки за скорость |
| ⚡ Все против всех (FFA) | Очки получает только первый правильный ответ |
| 🤝 Командный | Команды ходят по очереди, побеждает команда с большим счётом |

---

## Подключение с телефона

1. Убедись что телефон и компьютер в **одной Wi-Fi сети**
2. В терминале после запуска найди строку `По сети: http://192.168.x.x:5000`
3. Открой этот адрес в браузере телефона
4. Готово — можно вводить код комнаты и играть

---

## Требования

- Python **3.10+**
- Интернет-соединение (для GigaChat API)
- Аккаунт на [developers.sber.ru](https://developers.sber.ru/) для получения ключа

Все зависимости устанавливаются автоматически при запуске `run.py`.

---

## Переменные окружения (.env)

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `GIGACHAT_CREDENTIALS` | — | **Обязательно.** Ключ авторизации GigaChat |
| `GIGACHAT_SCOPE` | `GIGACHAT_API_PERS` | Скоуп API (для физлиц — PERS, для юрлиц — CORP) |
| `GIGACHAT_MODEL` | `GigaChat` | Модель (`GigaChat`, `GigaChat-Pro`) |
| `SECRET_KEY` | случайная | Секретный ключ Flask-сессий |
| `HOST` | `0.0.0.0` | Адрес для прослушивания (0.0.0.0 = все интерфейсы) |
| `PORT` | `5000` | Порт сервера |
| `LOG_LEVEL` | `INFO` | Уровень логов (`DEBUG`, `INFO`, `WARNING`) |

---

## Технологии

- **Backend:** Python + Flask + Flask-SocketIO + eventlet
- **AI:** GigaChat API (Сбербанк)
- **Frontend:** Vanilla JS + CSS + Socket.IO
- **Связь:** WebSocket (реальное время)
